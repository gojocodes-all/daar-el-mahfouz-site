# Daar El-Mahfouz Website

Frontend-only React + Vite website.

No backend. No MongoDB. No server folder.

## Run locally

```bash
cd daar-el-mahfouz-site
npm run install-all
npm run dev
```

Or:

```bash
cd client
npm install
npm run dev
```

## Deployment

Deploy the `client` folder to Vercel.
